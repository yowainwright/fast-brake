---
import { CopyButton } from "../common/CopyButton";
import { resolveDocsUrl } from "../../utils/urlResolver";
---

<div
  class="hero bg-base-100 text-base-content mx-auto py-12 sm:py-16 lg:py-20 px-4 sm:px-6 max-w-md md:max-w-full"
>
  <div class="hero-content text-center">
    <div class="w-full max-w-xl font-outfit">
      <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold">
        Install <span class="bg-clip-text text-transparent" style="background: linear-gradient(90deg, rgba(255, 69, 0, 1) 2%, rgba(255, 140, 0, 1) 55%, rgba(255, 165, 0, 1) 100%); -webkit-background-clip: text;">Fast Brake</span>
      </h1>

      <p class="mt-4 sm:mt-6 w-full max-w-lg text-center text-sm sm:text-base">
        Install fast-brake globally, as a devDependency,<br class="hidden sm:inline" />
        <span class="sm:hidden">or use with npx.</span>
        <span class="hidden sm:inline">or just use with npx.</span>
      </p>
      <div
        class="flex items-center bg-base-300 rounded-lg shadow-sm mt-4 justify-between p-1"
      >
        <code class="ml-3 text-sm sm:text-base">bun add -g fast-brake</code>
        <CopyButton client:only="react" />
      </div>

      <a href={resolveDocsUrl("setup")} class="inline-block mt-6 sm:mt-7 w-full sm:w-auto">
        <button class="btn btn-lg text-white border-0 hover:opacity-90 w-full sm:w-auto" style="background: linear-gradient(90deg, rgba(255, 69, 0, 1) 2%, rgba(255, 140, 0, 1) 55%, rgba(255, 165, 0, 1) 100%);">
          Setup guide
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-4">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5 21 12m0 0-7.5 7.5M21 12H3" />
          </svg>
        </button>
      </a>
    </div>
  </div>
</div>
